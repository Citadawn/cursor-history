# Task Completion Checklist

When completing a task, run these commands:

1. **Build**: `npm run build`
2. **Type Check**: `npm run typecheck`
3. **Lint**: `npm run lint`
4. **Test**: `npm test`
5. **Format** (if needed): `npm run format`

All should pass before committing.
